@model IEnumerable<hts.Entity.HastaTb>
@{
    ViewBag.Title = "HastaAnasayfa";
    Layout = "~/Views/Shared/HastaneLayout.cshtml";
}

<h2>HastaAnasayfa</h2>

<p>
    @Html.ActionLink("yeni ekle", "HastaOlusturma")
</p>
<table class="table">
    <tr>

        <th>
            Hasta Adı
        </th>
        <th>
            Doktor Adı
        </th>
        <th>
            @Html.DisplayNameFor(model => model.telefon)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.adres)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.hastaligi)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.adSoyad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.doktorTb.adSoyad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.telefon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.adres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.hastaligi)
            </td>
            <td>
                @Html.ActionLink("duzenle", "HastaDuzenleme", new { id = item.hastaTc }) |             
                @Html.ActionLink("sil", "HastaSilme", new { id = item.hastaTc })
            </td>
        </tr>
    }

</table>
